{%- comment -%}AUTO-GENERATED BY SPECTRUM STUDIO. DO NOT MODIFY.{%- endcomment -%}

{%- if sai_state != 'draft' and sai_state != 'live' -%}
  <!-- SAI:WRAPPER:STATE_DEFAULT input={{ sai_state | default: 'NIL' }} fell_back_to=live -->
  {%- assign sai_state = 'live' -%}
{%- endif -%}

{%- if sai_state == 'draft' -%}
  {%- assign sai_vis = 'on' -%}
{%- else -%}
  {%- assign sai_vis = 'off' -%}
{%- endif -%}

{%- if sai_state == 'draft' -%}
  {%- assign sai_handle = 'spectrum_snippet_frpxf1zz_bl368i1pytx07qejlhh52n2v_draft' -%}
{%- else -%}
  {%- assign sai_handle = 'spectrum_snippet_frpxf1zz_bl368i1pytx07qejlhh52n2v_live' -%}
{%- endif -%}

{%- assign sai_mo = metaobjects.spectrum_snippet_instance[sai_handle] -%}
{%- unless sai_mo -%}
  {%- paginate metaobjects.spectrum_snippet_instance.values by 250 -%}
    {%- for sai_entry in metaobjects.spectrum_snippet_instance.values -%}
      {%- if sai_entry.system.handle == sai_handle -%}
        {%- assign sai_mo = sai_entry -%}
        {%- break -%}
      {%- endif -%}
    {%- endfor -%}
  {%- endpaginate -%}
{%- endunless -%}

{%- if sai_mo and sai_mo.meta.value.status == 'active' -%}
  {%- assign sai_default = nil -%}
  {%- for sai_v in sai_mo.variants.value.variants -%}
    {%- if sai_v.isDefault -%}
      {%- assign sai_default = sai_v -%}
      {%- break -%}
    {%- endif -%}
  {%- endfor -%}

  {%- if sai_default -%}
    {%- capture sai_envelope -%}
    {
    "v":1,
    "instanceId":"bl368i1pytx07qejlhh52n2v",
    "snippetId":"frpxf1zz",
    "experienceId":"019f4bb6-d6cb-7b28-a801-507042e0c2b5"
    ,"variants":{{ sai_mo.variants.value.variants | json }}
    ,"pools":{
    }
    }
    {%- endcapture -%}

    <style data-spectrum-style="bl368i1pytx07qejlhh52n2v">
    {%- for sai_v in sai_mo.variants.value.variants -%}
      {%- if sai_v.css != blank -%}{{ sai_v.css }}{%- endif -%}
    {%- endfor -%}
    </style>
    <div data-spectrum-instance-id="bl368i1pytx07qejlhh52n2v" data-spectrum-snippet-id="frpxf1zz" data-spectrum-variant-id="{{ sai_default.variantId }}" data-spectrum-vis="{{ sai_vis }}" data-spectrum-lq-snippet="mut-bl368i1pytx07qejlhh52n2v" data-spectrum-experience="_system-178463768769" data-spectrum-feature-slug="pdp_related_products">
      <script type="application/json" data-spectrum-envelope>{{ sai_envelope | replace: '</script>', '<\/script>' }}</script>
      {% render '_sai-snippet-frpxf1zz', variant: sai_default, mo: sai_mo, mutation_handle: 'mut-bl368i1pytx07qejlhh52n2v', experience_handle: '_system-178463768769' %}
    </div>
  {%- endif -%}
{%- elsif sai_state == 'draft' -%}
  <div data-spectrum-instance-id="bl368i1pytx07qejlhh52n2v" data-spectrum-snippet-id="frpxf1zz" data-spectrum-vis="{{ sai_vis }}" data-spectrum-lq-snippet="mut-bl368i1pytx07qejlhh52n2v" data-spectrum-experience="_system-178463768769" data-spectrum-feature-slug="pdp_related_products">
    <!-- SAI:WRAPPER:EMPTY_DRAFT_MO_MISSING snippetId=frpxf1zz instanceId=bl368i1pytx07qejlhh52n2v -->
  </div>
{%- endif -%}