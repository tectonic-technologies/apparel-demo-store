{%- comment -%}AUTO-GENERATED BY SPECTRUM STUDIO. DO NOT MODIFY.{%- endcomment -%}

{%- if sai_state != 'draft' and sai_state != 'live' -%}
  <!-- SAI:WRAPPER:STATE_DEFAULT input={{ sai_state | default: 'NIL' }} fell_back_to=live -->
  {%- assign sai_state = 'live' -%}
{%- endif -%}

{%- if sai_state == 'draft' -%}
  {%- assign sai_vis = 'on' -%}
{%- else -%}
  {%- assign sai_vis = 'off' -%}
{%- endif -%}

{%- if sai_state == 'draft' -%}
  {%- assign sai_handle = 'spectrum_snippet_b309s2jg_prh6ni4812okowgfba32x1dg_draft' -%}
{%- else -%}
  {%- assign sai_handle = 'spectrum_snippet_b309s2jg_prh6ni4812okowgfba32x1dg_live' -%}
{%- endif -%}

{%- assign sai_mo = metaobjects.spectrum_snippet_instance[sai_handle] -%}
{%- unless sai_mo -%}
  {%- paginate metaobjects.spectrum_snippet_instance.values by 250 -%}
    {%- for sai_entry in metaobjects.spectrum_snippet_instance.values -%}
      {%- if sai_entry.system.handle == sai_handle -%}
        {%- assign sai_mo = sai_entry -%}
        {%- break -%}
      {%- endif -%}
    {%- endfor -%}
  {%- endpaginate -%}
{%- endunless -%}

{%- if sai_mo and sai_mo.meta.value.status == 'active' -%}
  {%- assign sai_default = nil -%}
  {%- for sai_v in sai_mo.variants.value.variants -%}
    {%- if sai_v.isDefault -%}
      {%- assign sai_default = sai_v -%}
      {%- break -%}
    {%- endif -%}
  {%- endfor -%}

  {%- if sai_default -%}
    {%- capture sai_envelope -%}
    {
    "v":1,
    "instanceId":"prh6ni4812okowgfba32x1dg",
    "snippetId":"b309s2jg",
    "experienceId":"019f4bb6-d6cb-7b28-a801-507042e0c2b5"
    ,"variants":{{ sai_mo.variants.value.variants | json }}
    ,"pools":{
    }
    }
    {%- endcapture -%}

    {%- assign sai_section_width = sai_default.content.section_width -%}
    <style data-spectrum-style="prh6ni4812okowgfba32x1dg">
    {%- for sai_v in sai_mo.variants.value.variants -%}
      {%- if sai_v.css != blank -%}{{ sai_v.css }}{%- endif -%}
    {%- endfor -%}
    </style>
    <div data-spectrum-instance-id="prh6ni4812okowgfba32x1dg" data-spectrum-snippet-id="b309s2jg" data-spectrum-variant-id="{{ sai_default.variantId }}" data-spectrum-vis="{{ sai_vis }}" data-spectrum-section-width="{{ sai_section_width }}" data-spectrum-lq-snippet="mut-prh6ni4812okowgfba32x1dg" data-spectrum-experience="_system-178463768769" data-spectrum-feature-slug="virtual_compare">
      <script type="application/json" data-spectrum-envelope>{{ sai_envelope | replace: '</script>', '<\/script>' }}</script>
      {% render '_sai-snippet-b309s2jg', variant: sai_default, mo: sai_mo, mutation_handle: 'mut-prh6ni4812okowgfba32x1dg', experience_handle: '_system-178463768769' %}
    </div>
  {%- endif -%}
{%- elsif sai_state == 'draft' -%}
  <div data-spectrum-instance-id="prh6ni4812okowgfba32x1dg" data-spectrum-snippet-id="b309s2jg" data-spectrum-vis="{{ sai_vis }}" data-spectrum-lq-snippet="mut-prh6ni4812okowgfba32x1dg" data-spectrum-experience="_system-178463768769" data-spectrum-feature-slug="virtual_compare">
    <!-- SAI:WRAPPER:EMPTY_DRAFT_MO_MISSING snippetId=b309s2jg instanceId=prh6ni4812okowgfba32x1dg -->
  </div>
{%- endif -%}